<Window x:Class="JetstyleTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JetstyleTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0" VerticalAlignment="Center">
            <TextBlock Text="Обрабатывать только основные пункты меню: "></TextBlock>
            <CheckBox x:Name="Checkbox"></CheckBox>
        </WrapPanel>

        <ListBox Margin="40" Grid.Row="1" x:Name="MenuHoverHistoryList" ItemsSource="{Binding Path=Items}"></ListBox>
    </Grid>
</Window>
